name: "Composer Package Test"
description: "Runs tests for a generic Composer package"

inputs:
  php_version:
    description: "PHP version used in the test run"
    required: true

  syntax_check_paths:
    description: "Paths for PHP syntax check (comma separated). If empty, syntax check is skipped."
    required: false

  test_suites:
    description: "Comma separated list of test suites (e.g. unit,integration). If empty, run all tests."
    required: false

  debug:
    required: false
    default: "false"

runs:
  using: "composite"
  steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ inputs.php_version }}
        tools: composer:v2

    - name: PHP syntax check
      shell: bash
      run: |
        if [[ -z "${{ inputs.syntax_check_paths }}" ]]; then
          echo "No syntax check paths provided - skipping PHP syntax check."
          exit 0
        fi

        IFS=',' read -ra PATHS <<< "${{ inputs.syntax_check_paths }}"

        for RAW_PATH in "${PATHS[@]}"; do
          PATH_TRIMMED="$(echo "$RAW_PATH" | xargs)"

          if [[ -z "$PATH_TRIMMED" ]]; then
            continue
          fi

          if [[ -d "$PATH_TRIMMED" ]]; then
            echo "Syntax check in $PATH_TRIMMED"
            find "$PATH_TRIMMED" -name "*.php" -print0 | xargs -0 -n1 php -l
          else
            echo "Path '$PATH_TRIMMED' does not exist - skipped."
          fi
        done

    - name: Install dependencies
      shell: bash
      run: |
        composer install --no-interaction --prefer-dist

    - name: Allow all plugins
      shell: bash
      run: |
        composer config allow-plugins true

    - name: Run PHPStan
      shell: bash
      if: ${{ hashFiles('phpstan.neon', 'phpstan.neon.dist') != '' }}
      run: |
        set +o pipefail

        if [ "${{ inputs.debug }}" = "true" ] || [ "${{ inputs.debug }}" = "1" ]; then
          OUTPUT_FILE=phpstan.log
          vendor/bin/phpstan analyse \
            --no-progress \
            --error-format=raw \
            >"$OUTPUT_FILE" 2>&1 || PHPSTAN_EXIT=$?
          echo "---- PHPStan output ----"
          cat "$OUTPUT_FILE"
        else
          vendor/bin/phpstan analyse \
            --no-progress \
            --quiet \
            2>&1 || PHPSTAN_EXIT=$?
        fi

        exit ${PHPSTAN_EXIT:-0}

    - name: Run PHPUnit
      shell: bash
      if: ${{ hashFiles('phpunit.xml') != '' }}
      run: |
        if [[ -z "${{ inputs.test_suites }}" ]]; then
          echo "No test suites specified - running default PHPUnit configuration"

          vendor/bin/phpunit \
            --fail-on-risky \
            --fail-on-warning \
            --no-coverage

        else
          IFS=',' read -ra SUITES <<< "${{ inputs.test_suites }}"

          for SUITE in "${SUITES[@]}"; do
        SUITE="$(echo "$SUITE" | xargs)"
            echo "Running test suite: $SUITE"

            vendor/bin/phpunit \
              --fail-on-risky \
              --fail-on-warning \
              --no-coverage \
              --testsuite="$SUITE"
          done
        fi
